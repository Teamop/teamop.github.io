---
const { translate: t } = Astro.locals

interface Props {
  showLeft?: boolean
  leftTitle?: string
  leftUrl?: string

  showRight?: boolean
  rightTitle?: string
  rightUrl?: string

  showPageCount?: boolean
  currentPage?: number
  totalPage?: number
}

const {
  showLeft = true,
  showRight = true,
  leftTitle,
  rightTitle,
  leftUrl,
  rightUrl,
  showPageCount = true,
  currentPage,
  totalPage,
} = Astro.props
---

<footer class="mt-5">
  {
    showPageCount && (
      <div class="mb-2.5">
        {t('page_number', currentPage)} / {t('page_count', totalPage)}
      </div>
    )
  }
  <div class="flex items-center gap-2">
    {
      showLeft && (
        <a title={leftTitle} href={leftUrl}>
          <span class="i-mdi-chevron-double-left inline-block h-4 w-4 vertical-mid" />
          {leftTitle}
        </a>
      )
    }
    {
      showRight && (
        <a title={rightTitle} href={rightUrl}>
          {rightTitle}
          <span class="i-mdi-chevron-double-right inline-block h-4 w-4 vertical-mid" />
        </a>
      )
    }
  </div>
</footer>
